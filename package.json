{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel extend --out-dir lib"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.9",
    "@babel/core": "^7.22.9",
    "@babel/preset-env": "^7.22.9"
  }
}
